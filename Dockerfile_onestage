# 使用 node:25-alpine 作为基础镜像
FROM node:25-alpine

# 设置工作目录
WORKDIR /app

# 复制根目录的 package.json 和 package-lock.json
# 这些文件包含了执行 'npm install' 和 'npm run build' 所需的脚本信息
COPY package*.json ./

# 复制 client 目录的 package.json 和 package-lock.json
COPY client/package*.json client/

# 安装所有依赖。根据根目录 package.json 的 'install' 脚本，这会安装 client 的依赖。
RUN npm install

# 复制整个 client 目录，包括源码
COPY client client/

# 构建前端应用。根据根目录 package.json 的 'build' 脚本
RUN npm run build

# 安装 serve，用于服务静态文件
RUN npm install -g serve

# 暴露容器端口
EXPOSE 3000

# 启动 serve 来托管构建好的 React 应用
CMD ["serve", "-s", "client/build", "-l", "3000"]
